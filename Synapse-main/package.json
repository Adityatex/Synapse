{
  "name": "synapse-root",
  "version": "1.0.0",
  "description": "Collaborative IDE Root",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "node scripts/dev.js"
  }
}
