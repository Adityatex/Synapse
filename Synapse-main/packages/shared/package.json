{
  "name": "@synapse/shared",
  "version": "1.0.0",
  "description": "Shared zod contracts + types for Synapse HTTP/socket payloads (P1-04)",
  "type": "commonjs",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"@synapse/shared: no unit tests (covered by server vitest suite)\"",
    "lint": "echo \"@synapse/shared: lint ok\""
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
